on:
  push:
    branches: [main, master]

name: deploy

jobs:
  deploy_functions:
    runs-on: ubuntu-latest

    steps:
      - uses: google-github-actions/setup-gcloud@master
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SA_KEY }}
          export_default_credentials: true

      - uses: actions/checkout@v2
        with:
          fetch-depth: 2

      - name: Get git diff
        id: git_diff
        shell: bash
        run: |
          echo ::set-output name=diff::$(git diff --name-only --diff-filter=AMDR @~..@ | grep "/" | cut -d"/" -f1 | uniq)

      - name: Check if needs running
        id: run_check
        shell: bash
        run: |
          if [[ ${{ steps.git_diff.outputs.diff }} == ".github" ]]; then
              to_run=false
          else
              to_run=true
          fi
          echo $to_run
          echo ::set-output name=needs_running::echo $to_run

      - name: Run Cloud Build
        shell: bash
        run: |
          for i in $(echo ${{ steps.git_diff.outputs.diff }}); do
            if [[ "$i" == ".github" ]]; then
              continue
            fi
            cd $i
                gcloud builds submit
            cd ..
          done
        if: ${{ steps.run_check.outputs.needs_running  == "true" }}
